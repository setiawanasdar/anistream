'use client';

/**
 * AuthButton – placeholder (fitur login via Supabase akan ditambahkan nanti).
 * Untuk sekarang, return null agar tidak memengaruhi layout.
 */
export default function AuthButton() {
  return null;
}
